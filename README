This is my .vim folder

It uses Vundle to manage plugins.

Remember to run :PluginInstall the first time and :PluginUpdate once in a while.
Can also run this the first time:

$ vim +PluginInstall +qall

